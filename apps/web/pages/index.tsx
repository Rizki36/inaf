import { Button } from "ui";
import { Page } from "../@types";

const Dashboard: Page = () => {
    return (
        <div>
            <h1>Web</h1>
            <Button />
        </div>
    );
};

export default Dashboard;
