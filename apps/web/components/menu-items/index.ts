import dashboard from "./dashboard";
import users from "./users";
import positions from "./positions";
import projects from "./projects";

const menuItems = {
    items: [dashboard, users, positions, projects],
};

export default menuItems;
